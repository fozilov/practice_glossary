<%= form_for(label_request) do |f| %>
  <% if label_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(label_request.errors.count, "error") %> prohibited this label_request from being saved:</h2>

      <ul>
      <% label_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
