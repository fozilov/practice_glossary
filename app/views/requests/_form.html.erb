<%= form_for(request) do |f| %>
  <% if request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
      <% request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_reason %>
    <%= f.text_area :request_reason %>
  </div>

  <div class="field">
    <%= f.label :requester %>
    <%= f.text_field :requester %>
  </div>

  <div class="field">
    <%= f.label :decision_reason %>
    <%= f.text_area :decision_reason %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
